# Tests

This directory is intentionally language-neutral. Replace this note with the project's test layout and command before the first behavior change.

Every behavior change starts with an executable failing test. Keep focused tests close to the module when the project's conventions call for it, and use this directory for integration or end-to-end tests when appropriate.